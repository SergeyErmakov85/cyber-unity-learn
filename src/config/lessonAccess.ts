/**
 * Последовательное открытие уроков в «Карте обучения» на главной.
 *
 * `false` (текущее состояние) — уроки открыты в любом порядке: читатель кликает
 *   любой раздел, не отмечая предыдущие как пройденные. Прогресс продолжает
 *   считаться, просто ничем не ограничивает навигацию.
 *
 * `true` — возвращается прежнее поведение: доступен только следующий урок после
 *   последнего пройденного, остальные показываются с замком и не кликаются.
 *
 * Это НЕ про монетизацию (см. `src/config/monetization.ts`) — здесь только
 * педагогическая последовательность прохождения.
 */
export const SEQUENTIAL_LESSON_LOCK = false;
