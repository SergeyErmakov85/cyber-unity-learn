/**
 * Единый выключатель монетизации.
 *
 * `false` — весь контент открыт всем посетителям, в том числе без регистрации:
 *   нет paywall (`ProGate` пропускает всех), нет меток FREE/PRO и замков,
 *   страница «Тарифы» не смонтирована и на неё нет ссылок. Из денежной темы
 *   на сайте остаётся только добровольное пожертвование на главной странице.
 *
 * `true` — возвращаются платные тарифы: маршрут `/pricing`, ссылки на него,
 *   PRO-гейт на уроках уровней 2–3, бейджи FREE/PRO и апселл-баннеры.
 *
 * Код монетизации намеренно НЕ удалён — чтобы снова включить платный доступ,
 * достаточно поменять значение этой константы на `true`.
 */
export const MONETIZATION_ENABLED = false;
