import CrossLinkToHub from "@/components/CrossLinkToHub";
import { SECTION_TITLE_CLASS, ProseP, KeyPoints, Callout, Code } from "./_shared";

const Section1 = () => (
  <>
    <h2 id="razdel-1-onnx" className={`${SECTION_TITLE_CLASS} scroll-mt-24`}>
      Раздел 1. ONNX: универсальный контейнер для обученной политики
    </h2>

    <ProseP>
      Во время обучения ML-Agents хранит сеть в формате PyTorch (<Code>.pt</Code>) — это «рабочая
      тетрадь» тренера: веса, состояние оптимизатора, всё для продолжения обучения. Но Unity на
      PyTorch не работает. Нужен нейтральный, кроссплатформенный формат, который понимает и
      Python-тренер на экспорте, и игровой движок на исполнении.
    </ProseP>

    <ProseP>
      Этот формат — <strong>ONNX</strong> (<em>Open Neural Network Exchange</em>): открытый стандарт
      описания вычислительного графа нейросети. ONNX-файл — это не «код для конкретного фреймворка»,
      а <strong>декларативный граф</strong>: список операторов (свёртки, матричные умножения,
      активации), их связи и веса. Любой движок, который умеет читать ONNX, может прогнать этот граф
      у себя — будь то PyTorch, браузер или Unity.
    </ProseP>

    <ProseP>Для нас важны три свойства ONNX:</ProseP>

    <ol className="space-y-3 my-4 list-decimal list-inside text-[15px] text-foreground/90 leading-relaxed">
      <li>
        <strong>Только инференс.</strong> В <Code>.onnx</Code> уезжают веса и структура для прямого
        прохода (forward), но не состояние оптимизатора. Дообучать <Code>.onnx</Code> нельзя — это
        «слепок» политики, а не тренировочный чекпойнт.
      </li>
      <li>
        <strong>Версионирование через opset.</strong> Набор операторов и их семантика фиксируются
        номером <em>opset version</em>. Инференс-движок Unity (Inference Engine / Sentis) на сегодня
        поддерживает модели ONNX с opset примерно от 7 до 25. ML-Agents экспортирует совместимый
        opset автоматически, так что вручную об этом думать не нужно — но если вы тащите чужую
        модель, opset придётся проверять.
      </li>
      <li>
        <strong>Фиксированные имена входов/выходов.</strong> В современных версиях ML-Agents все
        входы-наблюдения в <Code>.onnx</Code> идут с префиксом <Code>obs_</Code> (исчезло разделение
        на «векторные» и «визуальные» плейсхолдеры). Эти имена — контракт между моделью и движком:
        переименуете — инференс не загрузится.
      </li>
    </ol>

    <ProseP>
      Связь с тем, что вы уже знаете: ONNX-граф кодирует ровно ту функцию политики, которую обучал
      PPO (
      <CrossLinkToHub
        hubPath="/algorithms/ppo"
        hubAnchor="policy-network"
        hubTitle="Хаб PPO — политика как сеть"
      >
        ↗ Хаб: PPO → политика как сеть
      </CrossLinkToHub>
      ). Сам по себе ONNX — про сериализацию, не про RL.
    </ProseP>

    <Callout title="⚠️ Важно" color="amber">
      <Code>.onnx</Code> — это финальный продукт, а не точка возобновления. Если планируете
      дообучать — храните <Code>.pt</Code>-чекпойнты (см. Раздел 2). Потеряете их — сможете запускать
      модель, но не улучшать.
    </Callout>

    <KeyPoints
      items={[
        <>ONNX — открытый кроссплатформенный формат описания графа сети; нейтрален к фреймворку.</>,
        <>
          В <Code>.onnx</Code> уезжает только то, что нужно для прямого прохода: веса + структура,
          без состояния оптимизатора.
        </>,
        <>
          Совместимость задаётся <strong>opset</strong> (Unity Inference Engine: ~7–25) и{" "}
          <strong>именами входов</strong> (<Code>obs_…</Code>).
        </>,
        <>ML-Agents всё это проставляет сам; ручная сверка нужна только для моделей «со стороны».</>,
      ]}
    />
  </>
);

export default Section1;
